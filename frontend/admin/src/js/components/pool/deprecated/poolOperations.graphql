query getUpdatePoolData($id: ID!) {
  users {
    id
    email
    firstName
    lastName
  }
  classifications {
    id
    group
    level
  }
  cmoAssets {
    id
    key
    name {
      en
      fr
    }
  }
  pool(id: $id) {
    ...pool
  }
}
