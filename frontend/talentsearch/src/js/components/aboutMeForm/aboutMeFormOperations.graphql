query getAboutMe {
  me {
    id
    preferredLang
    currentProvince
    currentCity
    telephone
    firstName
    lastName
    email
    armedForcesStatus
    citizenship
    isProfileComplete
  }
}

mutation UpdateUserAsUser($id: ID!, $user: UpdateUserAsUserInput!) {
  updateUserAsUser(id: $id, user: $user) {
    preferredLang
    currentProvince
    currentCity
    telephone
    firstName
    lastName
    email
    isProfileComplete
    armedForcesStatus
    citizenship
  }
}
