query getMyStatus {
  me {
    id
    jobLookingStatus
    isProfileActive
  }
}

mutation UpdateMyStatus($id: ID!, $user: UpdateUserAsUserInput!) {
  updateUserAsUser(id: $id, user: $user) {
    jobLookingStatus
  }
}
