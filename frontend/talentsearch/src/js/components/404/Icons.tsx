import React from "react";
import Svg, { type SVGProps } from "@common/components/Svg/Svg";

const PugDark = (props: SVGProps) => <Svg {...props}>This will be a Pug!</Svg>;
const PugLight = (props: SVGProps) => <Svg {...props}>This will be a Pug!</Svg>;

export { PugDark, PugLight };
