query WorkLocationPreference($id: ID!) {
  user(id: $id) {
    id
    locationPreferences
    locationExemptions
  }
}

mutation createWorkLocationPreference($id: ID!, $user: UpdateUserAsUserInput!) {
  updateUserAsUser(id: $id, user: $user) {
    locationPreferences
    locationExemptions
  }
}
