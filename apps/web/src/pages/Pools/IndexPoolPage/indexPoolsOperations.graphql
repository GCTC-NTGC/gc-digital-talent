query allPools {
  pools {
    id
    owner {
      id
      email
      firstName
      lastName
    }
    name {
      en
      fr
    }
    classifications {
      id
      group
      level
    }
    status
    stream
    processNumber
    publishingGroup
    createdDate
    updatedDate
  }
}
