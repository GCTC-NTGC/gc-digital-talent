import { defineMessage } from "react-intl";

export const priorityTitle = defineMessage({
  defaultMessage: "Priority status",
  id: "66vzXk",
  description: "Title for a candidates priority status",
});

export const veteranTitle = defineMessage({
  defaultMessage: "Veteran status",
  id: "fI87Ov",
  description: "Title for a candidates veteran status",
});
