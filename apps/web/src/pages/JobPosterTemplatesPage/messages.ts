import { defineMessages } from "react-intl";

const messages = defineMessages({
  pageTitle: {
    defaultMessage: "Job templates",
    id: "k5Jueg",
    description: "Title for the 'job poster templates' page",
  },
});

export default messages;
