schema: http://127.0.0.1:8000/graphql
generates:
  ./src/types.d.ts:
    plugins:
      - typescript
    config:
      scalars:
        Date: string
        DateTime: string
        Email: string
        PhoneNumber: string
