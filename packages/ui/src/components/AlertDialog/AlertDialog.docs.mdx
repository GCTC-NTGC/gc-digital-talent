import { Story } from "@storybook/addon-docs";

# Alert Dialog

> A modal dialog that interrupts the user with important content and expects a response.

This is a styled version of [@radix-ui/alert-dialog](https://www.radix-ui.com/docs/primitives/components/alert-dialog). The exception being the `Footer` which is a custom component for styling purposes.

## Subcomponents

 - `AlertDialog.Root`
 - `AlertDialog.Content`
 - `AlertDialog.Trigger`
 - `AlertDialog.Title`
 - `AlertDialog.Description`
 - `AlertDialog.Footer`
 - `AlertDialog.Cancel`
 - `AlertDialog.Action`

## Usage

<Story id="components-alert-dialog--not-open" />

```tsx
import AlertDialog from "@gc-digital-talent/ui/AlertDialog";
import Button from "@gc-digital-talent/ui/Button";

const ExampleAlertDialog = () => (
  <AlertDialog.Root>
    <AlertDialog.Trigger>
      <Button>Open Alert Dialog</Button>
    </AlertDialog.Trigger>
    <AlertDialog.Content>
      <AlertDialog.Title>Example Alert Dialog</AlertDialog.Title>
      <AlertDialog.Description>Example Alert Dialog Description</AlertDialog.Description>
      <AlertDialog.Footer>
        <AlertDialog.Cancel>
          <Button color="white">Cancel</Button>
        </AlertDialog.Cancel>
        <AlertDialog.Action>
          <Button color="cta">Action</Button>
        </AlertDialog.Action>
      </AlertDialog.Footer>
    </AlertDialog.Content>
  </AlertDialog.Root>
);
```

## Links

 - [Documentation](https://www.radix-ui.com/docs/primitives/components/alert-dialog)
 - [Accessibility](https://www.w3.org/WAI/ARIA/apg/patterns/alertdialog/)
