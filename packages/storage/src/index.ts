import {
  getFromLocalStorage,
  setInLocalStorage,
  removeFromLocalStorage,
} from "./local";

import {
  getFromSessionStorage,
  setInSessionStorage,
  removeFromSessionStorage,
} from "./session";

export { getFromLocalStorage, setInLocalStorage, removeFromLocalStorage };
export { getFromSessionStorage, setInSessionStorage, removeFromSessionStorage };
