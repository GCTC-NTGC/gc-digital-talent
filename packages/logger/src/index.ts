import {
  tryParseSeverityLevelString,
  levelIncludes,
  getLoggingLevel,
} from "./utils/logger";

import useLogger, { defaultLogger } from "./hooks/useLogger";

export {
  tryParseSeverityLevelString,
  levelIncludes,
  getLoggingLevel,
  useLogger,
  defaultLogger,
};
