{
  "name": "@gc-digital-talent/date-helpers",
  "license": "AGPL-3.0",
  "version": "1.0.0",
  "main": "./dist/index.js",
  "types": "./dist/index.d.ts",
  "files": [
    "dist/**"
  ],
  "scripts": {
    "build": "tsup src/index.ts --silent --dts --external react --minify",
    "clean": "rm -rf .turbo && rm -rf node_modules && rm -rf dist",
    "dev": "tsup src/index.ys --silent --dts --extern react",
    "lint": "eslint \"src/**/*.ts*\"",
    "test": "jest --detectOpenHandles --forceExit --passWithNoTests",
    "watch": "tsup src/index.ts --watch --dts --external react --minify"
  },
  "jest": {
    "preset": "@gc-digital-talent/jest-presets/jest/node"
  },
  "dependencies": {
    "@gc-digital-talent/graphql": "workspace:*",
    "@gc-digital-talent/i18n": "workspace:*",
    "date-fns": "^2.30.0",
    "date-fns-tz": "^2.0.0",
    "react-intl": "^6.6.2"
  },
  "devDependencies": {
    "@gc-digital-talent/jest-presets": "workspace:*",
    "@swc/core": "^1.4.2",
    "@types/jest": "29.5.11",
    "eslint": "^8.56.0",
    "eslint-config-custom": "workspace:*",
    "jest": "^29.7.0",
    "tsconfig": "workspace:*",
    "tsup": "^8.0.2",
    "typescript": "^5.4.2"
  }
}
