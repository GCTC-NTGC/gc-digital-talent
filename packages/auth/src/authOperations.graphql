query getCurrentAuthorizedUser {
  myAuth {
    id
    email
    deletedDate
    roleAssignments {
      id
      role {
        id
        name
      }
      team {
        id
        name
      }
    }
  }
}
