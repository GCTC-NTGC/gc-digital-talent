query getCurrentAuthorizedUser {
  myAuth {
    id
    deletedDate
    roleAssignments {
      id
      role {
        id
        name
      }
      team {
        id
        name
      }
    }
  }
}
